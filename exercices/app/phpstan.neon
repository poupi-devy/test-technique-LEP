includes:
    - vendor/phpstan/phpstan-doctrine/extension.neon
    - vendor/phpstan/phpstan-symfony/extension.neon

parameters:
    level: 9
    paths:
        - src
        - tests
        - bin/console

    excludePaths:
        - */migrations
        - */vendor

    reportUnmatchedIgnoredErrors: true

    ignoreErrors:
        # Add specific ignoreErrors here if needed